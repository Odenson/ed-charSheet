# Claude Code local session/settings — but share project skills with the repo
.claude/*
!.claude/skills/
# ...and the preview launch config (static dev server for the app)
!.claude/launch.json

# Source spreadsheet (local only — source of truth for the importer)
Chakka-v7.13.xlsx

# Rulebook text extracts — local reference only (copyrighted FASA material,
# never commit / publish)
rulebook extracts/

# OS
.DS_Store

# Node
node_modules/
npm-debug.log*

# Local-only self-referential symlink for simulating the /dev/ instance
# (see README → "Simulating the dev instance locally"). Never committed.
/dev

# Character data lives on the character-data branch, not in the bundle
# (store.js loads it live on the Pages site). The gitignored working copies
# keep local dev / file:// working; fresh clones read from the branch instead.
data/character.json
data/characters.json
data/chakka.jpg
