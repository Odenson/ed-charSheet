{
  "name": "ed-charsheet-save-worker",
  "version": "0.1.0",
  "description": "Serverless save endpoint (Cloudflare Worker) for EDCharSheet — commits data/character.json to the character-data branch",
  "type": "module",
  "private": true,
  "license": "GPL-3.0",
  "scripts": {
    "test": "node --test",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
